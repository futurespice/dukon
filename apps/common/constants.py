# ---------------------------------------------------------------------------
# Shared constants used across multiple apps.
# Import from here — never redeclare in individual app files.
# ---------------------------------------------------------------------------

# Maximum number of IDs accepted in bulk operations (delete, update, etc.)
MAX_BULK_OPERATIONS = 100
