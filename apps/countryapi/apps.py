from django.apps import AppConfig


class CountryApiConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.countryapi'
    verbose_name = 'Гео (Страны / Регионы / Города)'
